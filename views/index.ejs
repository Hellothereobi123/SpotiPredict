<!DOCTYPE html>
<html>
<head>
    <!--<script type="text/javascript" src="index.js"></script>-->
</head>
<body>
    <a href = "/lols">
    <button>Something</button>
    </a>
    <p id="info_artist">
        <% if(tracks!=""){%>
        <%=JSON.stringify(tracks[0].name)%>
        <% }%>
    </p>
    <p id="info_artist">
        <% if(tracks!=""){%>
        <%=JSON.stringify(tracks[1].name)%>
        <% } %>
    </p>
    <p>
        <% if(tracks!=""){%>
            <%=JSON.stringify(tracks[2].name)%>
            <% } %>
    </p>
</body>
</html>
